---

- name: deploy
  synchronize: >
    archive=yes
    checksum=yes
    compress=yes
    delete=yes
    dest={{ deploy_to }}
    dest_port={{ ansible_ssh_port }}
    src={{ deploy_from }}
    recursive=yes
